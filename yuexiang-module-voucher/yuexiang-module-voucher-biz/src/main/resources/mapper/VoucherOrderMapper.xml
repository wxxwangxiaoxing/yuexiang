<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuexiang.voucher.mapper.VoucherOrderMapper">

    <select id="countByVoucherIdAndUserId" resultType="int">
        SELECT COUNT(1) 
        FROM tb_voucher_order 
        WHERE voucher_id = #{voucherId} 
          AND user_id = #{userId} 
          AND deleted = 0
    </select>

    <select id="selectOrderIdByVoucherAndUser" resultType="java.lang.Long">
        SELECT id 
        FROM tb_voucher_order 
        WHERE voucher_id = #{voucherId} 
          AND user_id = #{userId} 
          AND deleted = 0 
        LIMIT 1
    </select>

</mapper>
