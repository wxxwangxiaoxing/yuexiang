<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuexiang.user.mapper.SignMapper">

    <select id="selectByUserMonth" resultType="com.yuexiang.user.domain.entity.Sign">
        SELECT * 
        FROM tb_sign 
        WHERE user_id = #{userId} 
          AND year = #{year} 
          AND month = #{month} 
          AND deleted = 0
    </select>

    <update id="updateBitmap">
        UPDATE tb_sign 
        SET sign_bitmap = #{bitmap}, sign_days = #{signDays}, update_time = NOW() 
        WHERE id = #{id}
    </update>

</mapper>
