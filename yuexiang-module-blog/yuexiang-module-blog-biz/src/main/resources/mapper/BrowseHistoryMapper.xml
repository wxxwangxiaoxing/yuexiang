<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuexiang.blog.mapper.BrowseHistoryMapper">

    <insert id="insertOrUpdate" parameterType="com.yuexiang.blog.domain.entity.BrowseHistory" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_browse_history (user_id, biz_type, biz_id, view_time, view_count) 
        VALUES (#{userId}, #{bizType}, #{bizId}, NOW(), 1) 
        ON DUPLICATE KEY UPDATE view_time = NOW(), view_count = view_count + 1
    </insert>

</mapper>
