<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuexiang.blog.mapper.BlogLikeMapper">

    <delete id="deleteByUserAndBlog">
        DELETE FROM tb_blog_likes 
        WHERE user_id = #{userId} AND blog_id = #{blogId}
    </delete>

    <select id="countByUserAndBlog" resultType="int">
        SELECT COUNT(*) 
        FROM tb_blog_likes 
        WHERE user_id = #{userId} AND blog_id = #{blogId}
    </select>

    <select id="countByAuthorId" resultType="long">
        SELECT COUNT(*)
        FROM tb_blog_likes bl
        INNER JOIN tb_blog b ON bl.blog_id = b.id
        WHERE b.user_id = #{authorId} AND b.deleted = 0
    </select>

</mapper>
