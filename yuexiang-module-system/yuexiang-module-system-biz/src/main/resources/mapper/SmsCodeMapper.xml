<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuexiang.system.mapper.SmsCodeMapper">

    <resultMap id="BaseResultMap" type="com.yuexiang.system.domain.entity.SmsCode">
        <id column="id" property="id"/>
        <result column="phone" property="phone"/>
        <result column="code" property="code"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="ip" property="ip"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, phone, code, type, status, ip, expire_time, create_time
    </sql>

    <select id="selectLatest" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tb_sms_code
        WHERE phone = #{phone} AND type = #{type}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <update id="updateStatus">
        UPDATE tb_sms_code
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
